import * as theme from './theme';

// Global
export const inputTextColor = theme.textColor;
export const inputBorderColor = theme.borderColor;

// Font
export const fontSize = `17px`;
export const fontWeight = 400;
export const letterSpacing = 0;
export const textColor = theme.textColor;

// Input
export const boxShadowTextInput = `none`;
